import Foundation
import SwiftUI










struct CreditCardView:View {
	
	var body: some  View {

		
		VStack {
			Text("Text")
				.font(.title)
				.foregroundColor(Color.blue)
			
		}
		.frame(maxWidth: .infinity, maxHeight: .infinity)
		.background(.yellow)
		
		
		
	}
}






#Preview{
	CreditCardView()
}
